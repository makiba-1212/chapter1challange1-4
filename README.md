Project: Chapter1_Challenge_1_4

This Java program reads a config.txt file and validates its contents.

How it works

Reads the first line as a configuration version

Throws an error if the version is less than 2

Reads the second line as a file path

Checks if the file exists

Handles common errors (missing file, invalid number, old version)

config.txt format
